repos:
  - repo: local
    hooks:
    - id: black
      name: black
      language: python
      types_or: [python, pyi]
      entry: black --check
    - id: isort
      name: isort
      language: python
      types_or: [python, pyi]
      entry: isort --check
